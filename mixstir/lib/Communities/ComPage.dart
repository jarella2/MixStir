import 'package:flutter/material.dart';

class ComPage extends StatelessWidget {
  const ComPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text("This is what the communities page will look like.")
      
      );
  }
}